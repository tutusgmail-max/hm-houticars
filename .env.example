# Frontend only — never commit real keys to git
# Production app uses: ertdqfavrkomikszagtc (must match `npx supabase link` project-ref)
VITE_SUPABASE_URL=https://ertdqfavrkomikszagtc.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-public-key

# Vercel: Project → Settings → Environment Variables
# Add the same two variables for Production + Preview.
# Use the anon key from Supabase → Settings → API (NOT service_role).

# Supabase Auth → URL configuration:
# Site URL: https://your-production-domain.vercel.app
# Redirect URLs: http://localhost:5173, http://localhost:5174, /reset-password path on prod domain
# Auth → Email → Confirm email: OFF (instant signup session)

