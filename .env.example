# ──────────────────────────────────────────────────────────────
#  HM HOUTI CARS — Environment Variables
#  Copy to .env and fill in your values.
#  NEVER commit .env to git.
# ──────────────────────────────────────────────────────────────

# Required — Supabase project credentials (project: ertdqfavrkomikszagtc)
VITE_SUPABASE_URL=https://ertdqfavrkomikszagtc.supabase.co
VITE_SUPABASE_ANON_KEY=your-public-anon-key

# Production builds also read `.env.production` and src/config/supabaseEnv.js fallbacks.
# On Vercel, set the same VITE_* in Project → Settings → Environment Variables.

# Optional — WhatsApp number for booking confirmation (without +)
VITE_WHATSAPP_NUMBER=212611460900

# Optional — site metadata
VITE_SITE_NAME=HM Houti Cars
VITE_SITE_URL=https://hmhouticars.com
